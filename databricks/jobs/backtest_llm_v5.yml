resources:
  jobs:
    backtest_llm_v5:
      name: ${bundle.name}-backtest-llm-v5-${bundle.target}
      description: "LLM Backtest v5 — Stratified 300 samples + 7년 + Realistic baseline + Multi-metric"
      tasks:
        - task_key: backtest_llm_v5
          notebook_task:
            notebook_path: ../notebooks/job_backtest_llm_v5.py
            source: WORKSPACE
            base_parameters:
              n_per_zone: "100"
              seed: "42"
              smoke_test: "false"
              backtest_start: "2019-04-01"
              backtest_end: "2026-01-31"
          timeout_seconds: 3600
      max_concurrent_runs: 1
      tags:
        project: crude-compass
        layer: gold
        purpose: llm_backtest_v5
