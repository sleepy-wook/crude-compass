resources:
  jobs:
    backtest_analysis_v5:
      name: ${bundle.name}-backtest-analysis-v5-${bundle.target}
      description: "v5 Backtest 종합 분석 — per-zone, time split, multi-metric, counterfactual, calibration"
      tasks:
        - task_key: analysis
          notebook_task:
            notebook_path: ../notebooks/job_backtest_analysis_v5.py
            source: WORKSPACE
            base_parameters:
              run_id: "auto"
          timeout_seconds: 600
      max_concurrent_runs: 1
      tags:
        project: crude-compass
        layer: gold
        purpose: llm_backtest_analysis
