{
  "name": "crude-compass",
  "private": true,
  "version": "0.0.0",
  "description": "Root build wrapper for Databricks Apps — delegates to frontend/ npm build. Backend Python deps in requirements.txt (auto-installed by Apps).",
  "scripts": {
    "build": "cd frontend && npm ci --no-audit --no-fund && npm run build"
  }
}
