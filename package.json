{
  "name": "crude-compass",
  "private": true,
  "version": "0.0.0",
  "description": "Root build wrapper for Databricks Apps — delegates to frontend/ pnpm build. Backend Python deps in requirements.txt (auto-installed by Apps).",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "build": "cd frontend && pnpm install --frozen-lockfile && pnpm run build"
  },
  "devDependencies": {
    "pnpm": "10.33.4"
  }
}
